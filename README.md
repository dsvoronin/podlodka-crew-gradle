# Podlodka Android Crew gradle optimizations

- configs reuse
- variant filtering
- dependencies reuse (buildSrc?)
- pinning dependencies to repositories
- build scan shader tasks
- do not build tasks on sync
- build checks plugin